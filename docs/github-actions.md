Dependabot — автоматические обновления зависимостей + security alerts.

CodeQL (GitHub Code Scanning) — SAST (статический анализ уязвимостей).

pip-audit + Bandit + safety в CI — Python-ориентированные сканеры зависимостей и кода.

Secret scanning / Detect secrets — предотвратить попадание ключей в репозиторий.

Pre-commit: add security hooks — локальная ранняя защита.

Branch protection + required checks + CODEOWNERS — регламентация PR / review.

Security policy & issue templates — как сообщать уязвимости и кто отвечает.

Least-privilege for secrets & rotating keys — безопасное хранение секретов.

Optional: Trivy/Clair/Snyk integration later for container/image scanning (на будущее).
